# NA DEFINIÇÃO = PARÂMETROS (placeholders)
def somar(x, y):      # x, y = PARÂMETROS
    return x + y

# NA CHAMADA = ARGUMENTOS (valores reais)
somar(5, 3)           # 5, 3 = ARGUMENTOS por posição
somar(x=5, y=3)       # ARGUMENTOS nomeados